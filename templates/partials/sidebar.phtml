<aside id="sidebar" class="fixed top-0 left-0 z-40 w-64 h-screen pt-20 transition-transform -translate-x-full bg-white border-r border-gray-200 lg:translate-x-0 dark:bg-gray-900 dark:border-gray-700">
    <div class="h-full px-3 pb-4 overflow-y-auto bg-white dark:bg-gray-900">
        <ul class="space-y-2 font-medium">
            <?php foreach ($config['sidebar'] as $key => $value): ?>
                <?php if (is_array($value)): ?>
                    <details class="group" open>
                        <summary class="flex items-center p-2 text-indigo-600 rounded-lg dark:text-indigo-400 hover:bg-indigo-50 dark:hover:bg-indigo-900/50 cursor-pointer">
                            <svg class="w-4 h-4 mr-1 transition-transform group-open:rotate-90" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M8.25 4.5l7.5 7.5-7.5 7.5" />
                            </svg>
                            <span class="font-semibold"><?= $value['title'] ?></span>
                        </summary>
                        <ul class="mt-2 ml-4 space-y-1 border-l-2 border-indigo-100 dark:border-indigo-800">
                            <?php foreach ($value['items'] as $link => $title): ?>
                                <li>
                                    <a href="<?= $link ?>" class="flex items-center p-2 text-gray-700 rounded-lg dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 group">
                                        <span class="ml-3"><?= $title ?></span>
                                    </a>
                                </li>
                            <?php endforeach; ?>
                        </ul>
                    </details>
                <?php elseif (is_string($value)): ?>
                    <li>
                        <a href="<?= $key ?>" class="flex items-center p-2 text-gray-700 rounded-lg dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 group">
                            <span class="ml-3"><?= $value ?></span>
                        </a>
                    </li>
                <?php endif; ?>
            <?php endforeach; ?>
        </ul>
    </div>
</aside>